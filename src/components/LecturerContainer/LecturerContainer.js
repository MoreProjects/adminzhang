import React from 'react';
import './LecturerContainer.less';

const LecturerContainer = React.createClass({
    /**

     * 
     * @returns
     */
    render () {
        return (
            <div>
                a
            </div>
        );
    }
});

export default LecturerContainer;