import React from 'react';
import './LecturerMessageApp.less';

import LecturerMessageContainer from '../LecturerMessageContainer';

const LecturerMessageApp = React.createClass({
    /**

     * 
     * @returns
     */
    render () {
        return (
            <LecturerMessageContainer />
        );
    }
});

export default LecturerMessageApp;